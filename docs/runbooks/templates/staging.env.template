# Do not commit real staging.env
# Use GitHub Environment Secrets, server secret manager, systemd EnvironmentFile, Docker secrets, or cloud secret manager.
# Prefer IAM role over long-lived AWS keys.

APP_ENV=staging
APP_URL=https://staging.example.com
STAGING_BASE_URL=https://staging.example.com

DB_HOST=changeme
DB_PORT=3306
DB_DATABASE=changeme
DB_USERNAME=changeme
DB_PASSWORD=changeme

REDIS_HOST=changeme
REDIS_PORT=6379
REDIS_PASSWORD=changeme

BACKUP_S3_BUCKET=changeme
BACKUP_S3_PREFIX=restaurantpos/staging
AWS_REGION=changeme
AWS_ACCESS_KEY_ID=changeme
AWS_SECRET_ACCESS_KEY=changeme

MAIL_MAILER=smtp
MAIL_HOST=changeme
MAIL_PORT=587
MAIL_USERNAME=changeme
MAIL_PASSWORD=changeme
MAIL_FROM_ADDRESS=staging@example.com
NOTIFICATION_SMOKE_EMAIL=staging-smoke@example.com

SENTRY_LARAVEL_DSN=changeme
SENTRY_ENVIRONMENT=staging
OPS_ALERTS_WEBHOOK_URL=changeme

VNPAY_TMN_CODE=changeme
VNPAY_HASH_SECRET=changeme
VNPAY_IPN_URL=https://staging.example.com/api/v1/payments/vnpay/ipn

MOMO_PARTNER_CODE=changeme
MOMO_ACCESS_KEY=changeme
MOMO_SECRET_KEY=changeme
MOMO_IPN_URL=https://staging.example.com/api/v1/payments/momo/ipn
