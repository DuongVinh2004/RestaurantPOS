# Findings

| Bug ID | Area | Severity | Repro Steps | Expected | Actual | Evidence | Suspected Root Cause | Recommended Fix | Fixed in this batch? |
|---|---|---|---|---|---|---|---|---|---|
| BUG-UI-001 | Staff Web / Dashboard | High | 1. Navigate to `/login`<br>2. Login with UAT credentials<br>3. Open Cashier Shift if needed<br>4. Navigate to `/ops/dashboard` | Dashboard renders and displays `.staff-dashboard-page` | Dashboard page times out, `.staff-dashboard-page` is not visible. | Playwright Log (Task 217) | Missing API dependency, layout component crash, or cashier shift prerequisite not correctly propagating state in frontend. | Debug `/ops/dashboard` network requests or React component tree to identify why the main layout shell fails to render. | Yes (Batch 1) |
| BUG-UI-002 | Customer Web / Live e2e | Medium | 1. Start `npm run test:e2e:live`<br>2. Playwright navigates to `/` (home) | Home page displays expected text | Locator for "Browse the menu before your visit." times out and element is not found | Playwright Log (Task 214) | Mismatched localization copy in tests vs live code (Vietnamese vs English), or API `/api/v1/menu/items` blocked rendering of the hero section. | Update Playwright test assertions to match the actual localized UI string, or fix the hero section rendering logic. | Yes (Batch 1) |
| BUG-UI-003 | Runtime Scripts | Low | 1. Run `npm run runtime:up`<br>2. Cancel early<br>3. Run `npm run dev:all` | Should detect missing deps | `npm run dev:all` exits immediately if it detects process PIDs, even if processes are hung, failing to start Next.js and Vite correctly. | Console Logs | `local-runtime.mjs` / `dev-all.ps1` PID reuse logic is too aggressive and doesn't check health. | Add basic HTTP ping to the script before deciding a process is "reusable". | Yes (Batch 1) |
| BUG-UI-004 | Customer Web / Reservation Form | High | 1. Navigate to `/booking`<br>2. Fill out form details<br>3. Submit | Form allows branch selection and successful submission | E2E automation blocked. Playwright script misunderstood the 2-step booking flow and `BottomSheet` overlay blocked interactions. | Playwright trace/logs, `02-booking-form.png` | Playwright script assumed monolithic form instead of 2-step (hold then confirm) flow. `BottomSheet` was not explicitly closed and intercepted pointer events. Lack of stable `data-testid` prevented element targeting. | 1. Added `data-testid` to customer web elements.<br>2. Updated E2E script to handle 2-step flow and close `BottomSheet` overlay. | Yes (Batch 2) |
| BUG-UI-005 | Staff Web / Reservation Check-in | Medium | 1. Open Reservation details.<br>2. Click Check-in on a future reservation. | API should reject and UI should display a clear error toast. | The button does not disable and no error toast appears, leaving the operator confused. | Playwright execution logs (Failed Check-in) | Missing error handling / toast notification for 4xx responses on the Check-In mutation. | Add explicit toast on error for Check-in. | Yes (Batch 4) |
| BUG-UI-006 | E2E Scripts / UI Overlays | Low | 1. Run Playwright script.<br>2. Click a button under an overlay. | Click succeeds. | Playwright hangs indefinitely at `.click()` without an explicit timeout. | Playwright trace | `catch()` handlers swallow errors while the `.click()` action waits infinitely for element interaction. | Add explicit `{ timeout: 3000 }` to all `.click().catch()` patterns. | Yes (Batch 3) |
| BUG-UI-007 | Customer Web / Reservation Form | Medium | 1. Open Customer Web at late night.<br>2. Pick a duration that causes the reservation to end after closing time (23:59). | UI should gracefully reject or explain. | UI blocks submission with "Value must be greater than or equal to 30", but backend blocks tables, causing automation timeouts. | Screenshot (Test Failed) | Booking policy and business hours reject late-night check-ins; Playwright hardcoded 90 minutes. | Create `booking:qa-branch-247` command to extend hours, and reduce duration to 30 minutes in E2E script. | Yes (Batch 4) |
| BUG-UI-008 | QA Helper Command / Booking Policy | High | 1. Run `php artisan booking:qa-branch-247`<br>2. Fetch `GET /api/v1/restaurant/profile` or try to load Customer Web. | Returns 200 OK with Branch config. | Returns 422 mapped to 400 " The requested action violates a business rule.\ Customer web crashes. | Test logs / API Network trace | `qa-branch-247` command hardcoded `default_service_minutes = 15`. Backend strictly enforces 30 to 480 minutes, throwing ValidationException and aborting the context. | Change `default_service_minutes` to `30` in `qa-branch-247`. | Yes (Batch 5) |
| BUG-UI-009 | E2E Scripts / Ant Design Locators | Medium | 1. Run Playwright script.<br>2. Try to `.fill()` an input using `data-testid` wrapped by Ant Design's `InputNumber`. | Input is found and filled. | Playwright times out because `data-testid` is applied to the `.ant-input-number` wrapper, and the actual `<input>` element isn't directly reachable by that `data-testid` string without `.locator('input')`, or `InputNumber` doesn't pass it properly. | Playwright timeout error `waiting for getByTestId('inventory-po-line-quantity-input').locator('input')` | Ant Design `InputNumber` passes DOM attributes to its wrapper, not the inner `input`. | Use `.getByPlaceholder('SL')` or `.locator('.ant-input-number-input')` directly instead of relying solely on `data-testid`. | Yes (Batch 8) |
| BUG-UI-010 | E2E Scripts / Reservation Flow | Medium | 1. Run `order-lifecycle-deep-audit.spec.ts`.<br>2. Playwright navigates to Customer UI to create a reservation. | Reservation is created and redirected to detail page | Playwright times out at `waitForURL` after submitting reservation | Playwright test trace / log | Minimum lead time constraints or test timing issue blocking the redirect after submission, or test wait condition mismatch. | Update booking fixture dates to robustly bypass constraints, or use an API-driven setup for staff order testing. | Yes (Batch 10) |
| BUG-ORD-009 | Staff Web / Order Setup | Medium | 1. Create a reservation on a non-default branch (e.g., branch 5).<br>2. Login as admin with default branch context (branch 1).<br>3. Attempt to create order from UI. | UI should reliably sync branch context and open the order creation flow. | UI-driven create order can fail because the admin branch context differs from the reservation/table branch context. | Playwright trace / logs (Order Lifecycle Deep Audit) | Staff branch context switcher doesn't propagate deterministic branch scope to the table board or order creation when opening specific reservation IDs. | Align table board/order creation with reservation/table branch or expose deterministic navigation to branch-scoped table. | No |
| BUG-UI-011 | Customer Web / Preorder Flow | High | 1. Navigate to reservation detail page.<br>2. Preorder panel attempts to parse cart items using `preorderCartFromReservation(preorderQuery.data)`. | Preorder cart is parsed and populated successfully. | Frontend crashes with `TypeError: Cannot read properties of undefined (reading 'filter')`. | Playwright console error in `customer-self-service-deep-audit.spec.ts`. | The API response mock was missing the `normalized_pre_order_items` property inside the `pre_order` object, which is accessed by the cart mapper and mapped directly to `items.filter(...)` without a null safeguard. | Add `normalized_pre_order_items` property on preorder API mock. | Yes (Batch 11 / Customer self-service completion) |
| BUG-UI-012 | Customer Web / Error Alerts | Medium | 1. Modify preorder to trigger a 422 Row version conflict.<br>2. Click Update. | UI displays friendly Vietnamese conflict message `"Món đặt trước đã bị thay đổi bởi phiên làm việc khác. Vui lòng tải lại trang."`. | UI displays generic fallback `"Có lỗi xảy ra khi thực hiện thao tác."`. | Playwright trace / log. | In `preorder-panel.tsx`, `actionError instanceof Error` was used to verify if the error could be formatted. However, the custom API caller wraps and normalizes API errors into plain JavaScript objects (`NormalizedApiError`), which do not inherit from `Error`. | Use `normalizeApiError(actionError).message` directly to extract the clean Vietnamese user-facing message. | Yes (Batch 11 / Customer self-service completion) |
