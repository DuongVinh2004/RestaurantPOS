# Findings

| Bug ID | Area | Severity | Repro Steps | Expected | Actual | Evidence | Suspected Root Cause | Recommended Fix | Fixed in this batch? |
|---|---|---|---|---|---|---|---|---|---|
| BUG-UI-001 | Staff Web / Dashboard | High | 1. Navigate to `/login`<br>2. Login with UAT credentials<br>3. Open Cashier Shift if needed<br>4. Navigate to `/ops/dashboard` | Dashboard renders and displays `.staff-dashboard-page` | Dashboard page times out, `.staff-dashboard-page` is not visible. | Playwright Log (Task 217) | Missing API dependency, layout component crash, or cashier shift prerequisite not correctly propagating state in frontend. | Debug `/ops/dashboard` network requests or React component tree to identify why the main layout shell fails to render. | No |
| BUG-UI-002 | Customer Web / Live e2e | Medium | 1. Start `npm run test:e2e:live`<br>2. Playwright navigates to `/` (home) | Home page displays expected text | Locator for "Browse the menu before your visit." times out and element is not found | Playwright Log (Task 214) | Mismatched localization copy in tests vs live code (Vietnamese vs English), or API `/api/v1/menu/items` blocked rendering of the hero section. | Update Playwright test assertions to match the actual localized UI string, or fix the hero section rendering logic. | No |
| BUG-UI-003 | Runtime Scripts | Low | 1. Run `npm run runtime:up`<br>2. Cancel early<br>3. Run `npm run dev:all` | Should detect missing deps | `npm run dev:all` exits immediately if it detects process PIDs, even if processes are hung, failing to start Next.js and Vite correctly. | Console Logs | `local-runtime.mjs` / `dev-all.ps1` PID reuse logic is too aggressive and doesn't check health. | Add basic HTTP ping to the script before deciding a process is "reusable". | No |
