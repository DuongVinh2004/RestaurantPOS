# Module Unit Tests

Place new module-owned unit tests here:

```text
tests/Unit/Modules/<Domain>/...
```

Use the module name from `app/Modules/<Domain>` unless the behavior is still
canonically owned by a legacy path. Do not bulk-move existing tests here without
a focused code change.
